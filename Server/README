!IMPORTANT!

"npm run start"

After making any change you need to compile TS to JS:

1. Make sure you are at src dir and run terminal.
2. Write "tsc" and press enter
3. The compiled JS code will be placed in /build dir

To run server:

1. Go to build dir
2. Run in terminal: 'node app.js'
3. Server is running 